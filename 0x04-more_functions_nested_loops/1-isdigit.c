#include "main.h"

/**
 * _isdigit - check for a digit
 * @c: the  character to be checked
 * return: 1 for digit 0 for anything else
 */

int _isdigit(int c)

{
if (c >= 48 && c <= 57)
}
return (1);
}
return (0);
} 
